{% extends 'base.html.twig' %}

{% block title %}OrderProducts{% endblock %}

{% block body %}
    <h1>OrderProducts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_product.id }}</td>
            </tr>
            <tr>
                <th>Userorderid</th>
                <td>{{ order_product.userorderid }}</td>
            </tr>
            <tr>
                <th>Row</th>
                <td>{{ order_product.row }}</td>
            </tr>
            <tr>
                <th>Seat</th>
                <td>{{ order_product.seat }}</td>
            </tr>
            <tr>
                <th>TypePrice</th>
                <td>{{ order_product.typePrice }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ order_product.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_products_index') }}">back to list</a>

    <a href="{{ path('order_products_edit', {'id': order_product.id}) }}">edit</a>

    {{ include('order_products/_delete_form.html.twig') }}
{% endblock %}
