{% extends 'base.html.twig' %}

{% block title %}New Seans{% endblock %}

{% block body %}

    {% if is_granted('ROLE_ADMIN') %}

    <h1>Create new Seans</h1>

    {{ include('seans/_form.html.twig') }}

    <a href="{{ path('seans_index') }}">back to list</a>

    {% endif %}
{% endblock %}
