{% extends 'base.html.twig' %}

{% block title %}Edit Userorder{% endblock %}

{% block body %}
    <h1>Edit Userorder</h1>

    {{ include('userorder/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('userorder_index') }}">back to list</a>

    {{ include('userorder/_delete_form.html.twig') }}
{% endblock %}
